# AutoWallet

Virtual agent wallets. Frontend first.

**Stack:** Next.js (App Router) · TypeScript · React · Tailwind

Spend and fund update mock balances in the browser. A Postgres ledger (`packages/ledger`) is next — keep money logic out of route files when it lands.

```bash
npm run dev
```

Open [http://localhost:3000](http://localhost:3000). If Publix is already on 3000, Next will pick 3001.
